# Status API Environment

# The API reads from the local output directory
# Make sure ../../../output/last-run.json exists from scraper runs

# Optional: Vercel deployment environment
VERCEL_ENV=production
